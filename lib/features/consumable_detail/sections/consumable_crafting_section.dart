import 'package:flutter/material.dart';

class ConsumableCraftingSection extends StatelessWidget {
  const ConsumableCraftingSection({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Text("Crafting Section")
      ],
    );
  }
}
